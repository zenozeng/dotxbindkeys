(define (user-init)
  (display "USER::INIT")
  ;; (add-hook! reset-keys-hook
  ;;            (lambda ()
  ;;              (xbindkey-function '(control q)
  ;;                                 (dosomething))))
  )
(user-init)
